{
  "name": "@reactive-class/react",
  "packageManager": "yarn@3.0.2",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "yarn run compile",
    "pretest": "yarn run compile",
    "posttest": "yarn run lint"
  },
  "devDependencies": {
    "@types/node": "^16.10.3",
    "sijiaoh-gts": "^0.5.0",
    "typescript": "^4.4.3"
  }
}
