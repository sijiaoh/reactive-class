{
  "name": "react-class",
  "private": true,
  "packageManager": "yarn@3.0.2",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "g:publish": "yarn workspaces foreach --parallel --topological-dev --no-private npm publish",
    "g:clear": "yarn workspaces foreach --parallel run clear",
    "g:compile": "yarn workspaces foreach --parallel run compile",
    "g:test": "yarn workspaces foreach --parallel run test",
    "g:fix": "yarn workspaces foreach --parallel run fix"
  }
}
